<Window x:Class="graphic_editor.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:graphic_editor"
        mc:Ignorable="d"
        Title="OOP Graphic Editor" Height="600" Width="800" 
        KeyDown="Window_KeyDown" KeyUp="Window_KeyUp">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Menu Grid.Row="0">
            <MenuItem Header="_File">
                <MenuItem Header="_Save" Click="Save_Click"/>
                <MenuItem Header="_Load" Click="Load_Click"/>
            </MenuItem>
            <MenuItem Header="_Edit">
                <MenuItem Header="_Undo" Click="Undo_Click"/>
                <MenuItem Header="_Delete" Click="Delete_Click"/>
            </MenuItem>
            <MenuItem Header="_View">
                <MenuItem Header="_Zoom In" Click="ZoomIn_Click"/>
                <MenuItem Header="_Zoom Out" Click="ZoomOut_Click"/>
                <MenuItem Header="_Reset Zoom" Click="ResetZoom_Click"/>
            </MenuItem>
        </Menu>

        <ToolBar Grid.Row="1" Height="40">
            <Button Content="Select" Click="SelectTool_Click"/>
            <Button Content="Rect" Click="RectangleTool_Click"/>
            <Button Content="Ellipse" Click="EllipseTool_Click"/>
            <Button Content="Line" Click="LineTool_Click"/>
            <Button Content="Polygon" Click="PolygonTool_Click"/>
            <Separator/>

            <TextBlock Text=" Fill:" VerticalAlignment="Center" Margin="5,0"/>
            <local:ColorPicker x:Name="FillColorPicker" 
               Width="80" 
               SelectedColorChanged="FillColor_Changed"/>

            <Separator/>

            <TextBlock Text=" Stroke:" VerticalAlignment="Center" Margin="5,0"/>
            <local:ColorPicker x:Name="StrokeColorPicker" 
               Width="80" 
               SelectedColorChanged="StrokeColor_Changed"/>
            <Separator/>

            <TextBlock Text=" Zoom:" VerticalAlignment="Center" Margin="5,0"/>
            <TextBlock x:Name="ZoomText" Text="100%" VerticalAlignment="Center" Width="40"/>
        </ToolBar>

        <Border Grid.Row="2" BorderBrush="Black" BorderThickness="1" Margin="5" ClipToBounds="True">
            <local:MyCanvas x:Name="Cava"
                            Background="Beige"
                            MouseDown="Cava_MouseDown"
                            MouseMove="Cava_MouseMove"
                            MouseUp="Cava_MouseUp"
                            MouseWheel="Cava_MouseWheel"/>
        </Border>
    </Grid>
</Window>